---
story_key: GA-SI-002-S03
parent_epic: GA-SI-002
project: sales_insights
labels: [frontend, architecture, angular]
components: [frontendapp]
---

# Story: Angular Dashboard Module Bootstrap (Routing, Base Components)

## Implementation Scope
Extend the robust `frontendapplication-i2oretail` SPA. Implement the new `SalesInsightsModule` as an Angular (15) lazy-loaded module mirroring the existing `/sales-analysis/` conventions. Configure the sidebar navigation item, baseline layout routes (`app-routing.module.ts`), and set up the `PrimeNG` + `Angular Material` container framework.

## Acceptance Criteria
- [ ] Provide lazy route `path: 'sales-insights', loadChildren: () => import(...)`.
- [ ] Connect `RoleBasedAuthGuard` verifying JWT payload matches access logic.
- [ ] Scaffold `SalesFilterBarComponent`, `KpiCardGroupComponent`, `TrendChartComponent`, `SalesDataGridComponent` with base `scss` padding constraints matching the i2o corporate standard.
- [ ] Append navigation sidebar entry exclusively showing for Sales Insight / Admin permission roles.

## Execution Directives
- Base: `src/app/modules/sales-insights/` inside `/frontendapplication-i2oretail/` repository.
- Rely strictly on existing Corporate style-guide palettes for SCSS maps.
